### 🔍 Hallazgos en tu backend (para corregir después)

| ID  | Gravedad   | Descripción                                                                                                                                                     | Ubicación                                                                                  |
| --- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| H1  | 🔴 Crítica | Creación de gastos sin verificar pertenencia al grupo. Cualquier usuario autenticado puede inyectar gastos en grupos ajenos.                                    | `ExpenseService.create` — falta hook `verifyGroupMembership`                               |
| H2  | 🔴 Crítica | `groupMember.controller.ts` no pasa `provider` a las llamadas al servicio. Las operaciones de miembros se ejecutan como internas, sin verificación de permisos. | `src/controllers/groupMember.controller.ts` — añadir `provider: req.provider`              |
| H3  | ⚠️ Media   | IDOR horizontal de lectura en grupos: cualquier usuario puede ver detalles y miembros de grupos a los que no pertenece.                                         | `GroupService.get` y `GroupService.find` — falta `checkOwnership` o filtro por pertenencia |
| H4  | ⚠️ Media   | IDOR horizontal de lectura en gastos: cualquier usuario puede ver gastos de grupos a los que no pertenece.                                                      | `ExpenseService.get` y `ExpenseService.find` — falta filtro por pertenencia al grupo       |
| H5  | ℹ️ Baja    | Stack trace y `X-Powered-By: Express` en respuestas de error (solo relevante en producción).                                                                    | Middleware de error y `app.disable('x-powered-by')`                                        |
